# Set this to your deployed backend's URL + /api before building for production.
# Example (Render): VITE_API_URL=https://roveon-backend.onrender.com/api
# Leave unset for local development — Vite's dev proxy handles /api automatically.
VITE_API_URL=
